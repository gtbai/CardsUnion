<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-12">
    <div class="field input-group input-group-lg ">
      <!--<%= f.label :balance %>-->
      <%= f.text_field :balance, placeholder: "Current Balance", class: "form-control"%>
    </div>
    <div class="input-group input-group-lg">
      <!--<%= label_tag :consumer , "Consumer's Phone Number"%>-->
      <%= text_field_tag :consumer, nil, class: "form-control", id: "demo1", autocomplete: "off", placeholder: "Phone Number"%>
    </div>
    <br />
    <div class="actions">
      <%= f.submit class: "btn btn-info btn-block btn-lg"%>
    </div>

  <!--This is Bai's Version of AutoCompletion, Comment here to push to dev in case of future needs-->
  <!--<div class="field">
    <%= f.label :balance %><br />
    <%= f.text_field :balance %>
  </div>
  <div class="input-group input-group-lg">
    <%= label_tag :consumer , "Consumer's Phone Number"%><br />
    <%= text_field_tag :consumer, nil, class: "form-control", autocomplete: "off"%>
  </div>-->

  <!--used for autocomplete -->
  <script type="text/javascript">
  $('#consumer').typeahead({
          name: 'phone',
          remote: '/cards/autocomplete?query=%QUERY'
      });
  </script>
  <script type="text/javascript"></script>
  <script src="../assets/jquery.js"></script>
<% end %>